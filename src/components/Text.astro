---
// Henter parametrene fra Astro.props
const { teksttype="brødtekst", stylingclasses="", weight=""} = Astro.props; 
 
//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
"brødtekst": "text-[12px] md:text-[20px]",
}

const fontWeight = {
"regular": "font-normal",
"bold": "font-bold",
"italic": "italic",
};
---
<p class:list={[sizeClasses[teksttype], stylingclasses, fontWeight[weight]]}><slot /></p>


